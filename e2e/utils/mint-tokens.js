// @flow

export async function mintTokens(paymentAddress: string) {
  // check if we have pem file available in directory connectme
  // if not, then throw an error with very helpful message
  // once we know that file exist, then go ahead
  // create a mint token transaction file
  // have a format ready for file
  // write to file with payment address
  // connect to ssh
  // upload mint file to ssh
  // execute command indy mint-automated-test
}
